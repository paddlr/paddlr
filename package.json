{
  "name": "paddlr",
  "version": "1.0.0",
  "description": "ping pong brilliance",
  "main": "backend/index.js",
  "engines": {
    "node": "8.12.0"
  },

  "scripts": {
    "start": "node backend/index.js",
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "heroku-postbuild": "cd frontend && npm run build & npm start"
  }
}
